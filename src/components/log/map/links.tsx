import { TStations } from 'utils/types/runData';

function Links({ Stations }: { Stations: TStations }) {
  return null;
// TODO: LINK


//   const anchors = Stations.map(Station => {
//     const { Type } = Station;
//     // const [x, y] = MapNodes.node(X, Y);

//     return (
//       // <a className="c-map-stations__station" key={`Act${Act}_x${X}y${Y}`} style={{ left: x, top: y}}>
//         Type
//       // </a>
//     )
//   });

//   return (
//     <div className="c-map-stations">
//       {anchors}
//     </div>
//   );
}

export default Links;